name: Bot-Roma
on:
workflow_dispatch:
Riga 4: jobs:
Riga 5:   invio:
Riga 6:     runs-on: ubuntu-latest
Riga 7:     steps:
- run: curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} -d text="✅ Dr. Confalone, assistente ATTIVO per Roma"
